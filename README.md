# MarketDuck Web Client

## start script

```bash
# install
npm i -g pnpm
pnpm install --frozen-lockfile
```

## add library

```bash
# add
pnpm add <library_name>

# remove
pnpm remove <library_name>
```
